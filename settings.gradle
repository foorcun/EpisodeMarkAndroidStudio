pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "EpisodeMark"
include ':app'
include ':common'
include ':common:common-domain'
include ':app:series-module'
include ':app:series-module:series-domain'
include ':app:series-module:series-domain:series-domain-core'
